#!/usr/bin/env python3
# *coding: utf-8

def shakutori():
    right = 0
    sum = 0
    for i in range(0, N):
        while right < N and (right を1個進めたときに条件をみたす):
            # 実際にrightを1進める
            # ex: sum += a[right]
            right += 1

        # break した状態でrightは条件を満たす最大なので、何かする
        # ex: res += (right - left)

        # left を右へ1ずらす準備
        # ex: if right == left: right += 1
        # ex: else sum -= a[left]
